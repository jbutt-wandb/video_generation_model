[project]
name = "video-generation-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.14.0",
    "av>=17.1.0",
    "diffusers>=0.39.0",
    "imageio>=2.37.4",
    "imageio-ffmpeg>=0.6.0",
    "opencv-python>=5.0.0.93",
    "python-dotenv>=1.2.2",
    "torch>=2.13.0",
    "torchvision>=0.28.0",
    "transformers>=5.14.1",
    "wandb>=0.28.1",
]

[tool.uv.sources]
# Pinned: MiniMax-H3 modular-pipeline support lives on main and is not in a
# stable release yet; this is the commit the cluster venv was built from.
diffusers = { git = "https://github.com/huggingface/diffusers.git", rev = "90c0ffdc045902a3667d473d2fbfc03e8716dba9" }
